{
  "name": "@nischoy/topo-servicenow-control-plane",
  "version": "0.4.3",
  "description": "ServiceNow control plane for stateless Nischoy Topo workers",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "now-sdk build",
    "test": "node tests/target_scope_planner.test.js && node tests/password2_ssh_contract.test.js",
    "deploy": "now-sdk install",
    "pack": "now-sdk pack"
  },
  "devDependencies": {
    "@servicenow/sdk": "4.9.0"
  }
}
