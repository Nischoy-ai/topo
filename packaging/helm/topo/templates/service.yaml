apiVersion: v1
kind: Service
metadata:
  name: {{ include "topo.fullname" . }}
  labels:
    {{- include "topo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "topo.selectorLabels" . | nindent 4 }}
