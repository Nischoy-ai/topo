Topo Hub is installed as {{ include "topo.fullname" . }}.

The chart used the existing Secret {{ .Values.apiKeySecret.name }}. Helm never
stores the API key value. Before an application upgrade, create and verify a
SQLite backup as described in docs/storage.md.
